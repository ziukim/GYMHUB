<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.gymhub.model.mapper.NoticeMapper">
    <resultMap id="noticeResultMap" type="GymNotice">
        <result column="NOTICE_NO" property="noticeNo"/>
        <result column="NOTICE_TITLE" property="noticeTitle"/>
        <result column="NOTICE_WRITER" property="noticeWriter"/>
        <result column="NOTICE_CATEGORY" property="noticeCategory"/>
        <result column="NOTICE_CONTENT" property="noticeContent"/>
        <result column="NOTICE_DATE" property="noticeDate"/>
        <result column="NOTICE_FIX_STATUS" property="noticeFixStatus"/>
        <result column="NOTICE_FILE_PATH" property="filePath"/>
        <result column="GYM_NO" property="gymNo"/>
    </resultMap>

    <select id="selectNoticesByGymNo" parameterType="int" resultMap="noticeResultMap">
        SELECT 
            NOTICE_NO,
            NOTICE_TITLE,
            NOTICE_WRITER,
            NOTICE_CATEGORY,
            NOTICE_CONTENT,
            NOTICE_DATE,
            NOTICE_FIX_STATUS,
            NOTICE_FILE_PATH,
            GYM_NO
        FROM GYM_NOTICE
        WHERE GYM_NO = #{gymNo}
        ORDER BY 
            CASE WHEN NOTICE_FIX_STATUS = 'Y' THEN 0 ELSE 1 END,
            NOTICE_DATE DESC
    </select>

    <select id="selectNoticeByNo" parameterType="int" resultMap="noticeResultMap">
        SELECT 
            NOTICE_NO,
            NOTICE_TITLE,
            NOTICE_WRITER,
            NOTICE_CATEGORY,
            NOTICE_CONTENT,
            NOTICE_DATE,
            NOTICE_FIX_STATUS,
            NOTICE_FILE_PATH,
            GYM_NO
        FROM GYM_NOTICE
        WHERE NOTICE_NO = #{noticeNo}
    </select>

    <insert id="insertNotice" parameterType="GymNotice">
        <selectKey keyProperty="noticeNo" resultType="_int" order="BEFORE">
            SELECT SEQ_NOTICE_NO.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO GYM_NOTICE (
            NOTICE_NO,
            NOTICE_TITLE,
            NOTICE_WRITER,
            NOTICE_CATEGORY,
            NOTICE_CONTENT,
            NOTICE_DATE,
            NOTICE_FIX_STATUS,
            NOTICE_FILE_PATH,
            GYM_NO
        ) VALUES (
            #{noticeNo},
            #{noticeTitle},
            #{noticeWriter},
            #{noticeCategory},
            #{noticeContent},
            SYSDATE,
            #{noticeFixStatus},
            #{filePath},
            #{gymNo}
        )
    </insert>

    <update id="updateNotice" parameterType="GymNotice">
        UPDATE GYM_NOTICE
        SET NOTICE_TITLE = #{noticeTitle},
            NOTICE_WRITER = #{noticeWriter},
            NOTICE_CATEGORY = #{noticeCategory},
            NOTICE_CONTENT = #{noticeContent},
            NOTICE_FILE_PATH = #{filePath}
        WHERE NOTICE_NO = #{noticeNo}
    </update>

    <update id="updateNoticeFixStatus">
        UPDATE GYM_NOTICE
        SET NOTICE_FIX_STATUS = #{fixStatus}
        WHERE NOTICE_NO = #{noticeNo}
    </update>
</mapper>

